name: WLED compile

on:
    workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.9

    - name: Install PlatformIO
      run: |
        python -m pip install --upgrade pip
        pip install --upgrade platformio
    - name: Run PlatformIO d1_mini
      run: |
        rm /tmp/mm.zip || true
        wget https://github.com/Aircoookie/WLED/archive/refs/heads/main.zip -O /tmp/mm.zip
        unzip -o /tmp/mm.zip -d /tmp
        rm /tmp/wledfoldername || true
        find /tmp -maxdepth 1 -name "WLED-*" -type d >> /tmp/wledfoldername
        cd `cat /tmp/wledfoldername`
        echo '[env:custom_build]' >>platformio.ini
        echo 'extends = env:d1_mini' >>platformio.ini
        echo 'build_flags = ${env:d1_mini.build_flags} ${common.debug_flags}' >>platformio.ini
        
        pio run -e custom_build || pio run -e custom_build || true
    - name: Copy Files d1_mini
      run: |
        cd $GITHUB_WORKSPACE
        WLEDFOLDERNAME=`cat /tmp/wledfoldername`
        cp ${WLEDFOLDERNAME}/build_output/firmware/custom_build.bin $GITHUB_WORKSPACE/custom_build_d1_mini.bin || true
        date -I | tr -d '\n' >$GITHUB_WORKSPACE/last_update_date.txt
    - name: Commit Files d1_mini
      run: |
        cd $GITHUB_WORKSPACE
        git config user.name "GitHub Actions Bot"
        git config user.email "<>"
        git add $GITHUB_WORKSPACE/custom_build_d1_mini.bin || true
        git add $GITHUB_WORKSPACE/last_update_date.txt || true
        git commit -m "Commit Custom Build" || true
        git push origin main || true
    - name: Run PlatformIO esp32dev_qio80
      run: |
        rm /tmp/mm.zip || true
        wget https://github.com/Aircoookie/WLED/archive/refs/heads/main.zip -O /tmp/mm.zip
        unzip -o /tmp/mm.zip -d /tmp
        rm /tmp/wledfoldername || true
        find /tmp -maxdepth 1 -name "WLED-*" -type d >> /tmp/wledfoldername
        cd `cat /tmp/wledfoldername`
        echo '[env:custom_build]' >>platformio.ini
        echo 'extends = env:esp32dev_qio80' >>platformio.ini
        echo 'build_flags = ${env:esp32dev_qio80.build_flags} ${common.debug_flags}' >>platformio.ini
        
        pio run -e custom_build || pio run -e custom_build || true
    - name: Copy Files esp32dev_qio80
      run: |
        cd $GITHUB_WORKSPACE
        WLEDFOLDERNAME=`cat /tmp/wledfoldername`
        cp ${WLEDFOLDERNAME}/build_output/firmware/custom_build.bin $GITHUB_WORKSPACE/custom_build_esp32dev_qio80.bin || true
        date -I | tr -d '\n' >$GITHUB_WORKSPACE/last_update_date.txt
    - name: Commit Files esp32dev_qio80
      run: |
        cd $GITHUB_WORKSPACE
        git config user.name "GitHub Actions Bot"
        git config user.email "<>"
        git add $GITHUB_WORKSPACE/custom_build_esp32dev_qio80.bin || true
        git add $GITHUB_WORKSPACE/last_update_date.txt || true
        git commit -m "Commit Custom Build" || true
        git push origin main || true
    - name: Run PlatformIO esp32dev_V4_dio80
      run: |
        rm /tmp/mm.zip || true
        wget https://github.com/Aircoookie/WLED/archive/refs/heads/main.zip -O /tmp/mm.zip
        unzip -o /tmp/mm.zip -d /tmp
        rm /tmp/wledfoldername || true
        find /tmp -maxdepth 1 -name "WLED-*" -type d >> /tmp/wledfoldername
        cd `cat /tmp/wledfoldername`
        echo '[env:custom_build]' >>platformio.ini
        echo 'extends = env:esp32dev_V4_dio80' >>platformio.ini
        echo 'build_flags = ${env:esp32dev_V4_dio80.build_flags} ${common.debug_flags}' >>platformio.ini
        
        pio run -e custom_build || pio run -e custom_build || true
    - name: Copy Files esp32dev_V4_dio80
      run: |
        cd $GITHUB_WORKSPACE
        WLEDFOLDERNAME=`cat /tmp/wledfoldername`
        cp ${WLEDFOLDERNAME}/build_output/firmware/custom_build.bin $GITHUB_WORKSPACE/custom_build_esp32dev_V4_dio80.bin || true
        date -I | tr -d '\n' >$GITHUB_WORKSPACE/last_update_date.txt
    - name: Commit Files esp32dev_V4_dio80
      run: |
        cd $GITHUB_WORKSPACE
        git config user.name "GitHub Actions Bot"
        git config user.email "<>"
        git add $GITHUB_WORKSPACE/custom_build_esp32dev_V4_dio80.bin || true
        git add $GITHUB_WORKSPACE/last_update_date.txt || true
        git commit -m "Commit Custom Build" || true
        git push origin main || true
    - name: Run PlatformIO esp32_eth
      run: |
        rm /tmp/mm.zip || true
        wget https://github.com/Aircoookie/WLED/archive/refs/heads/main.zip -O /tmp/mm.zip
        unzip -o /tmp/mm.zip -d /tmp
        rm /tmp/wledfoldername || true
        find /tmp -maxdepth 1 -name "WLED-*" -type d >> /tmp/wledfoldername
        cd `cat /tmp/wledfoldername`
        echo '[env:custom_build]' >>platformio.ini
        echo 'extends = env:esp32_eth' >>platformio.ini
        echo 'build_flags = ${env:esp32_eth.build_flags} ${common.debug_flags}' >>platformio.ini
        
        pio run -e custom_build || pio run -e custom_build || true
    - name: Copy Files esp32_eth
      run: |
        cd $GITHUB_WORKSPACE
        WLEDFOLDERNAME=`cat /tmp/wledfoldername`
        cp ${WLEDFOLDERNAME}/build_output/firmware/custom_build.bin $GITHUB_WORKSPACE/custom_build_esp32_eth.bin || true
        date -I | tr -d '\n' >$GITHUB_WORKSPACE/last_update_date.txt
    - name: Commit Files esp32_eth
      run: |
        cd $GITHUB_WORKSPACE
        git config user.name "GitHub Actions Bot"
        git config user.email "<>"
        git add $GITHUB_WORKSPACE/custom_build_esp32_eth.bin || true
        git add $GITHUB_WORKSPACE/last_update_date.txt || true
        git commit -m "Commit Custom Build" || true
        git push origin main || true
    - name: Run PlatformIO esp32s2_saola
      run: |
        rm /tmp/mm.zip || true
        wget https://github.com/Aircoookie/WLED/archive/refs/heads/main.zip -O /tmp/mm.zip
        unzip -o /tmp/mm.zip -d /tmp
        rm /tmp/wledfoldername || true
        find /tmp -maxdepth 1 -name "WLED-*" -type d >> /tmp/wledfoldername
        cd `cat /tmp/wledfoldername`
        echo '[env:custom_build]' >>platformio.ini
        echo 'extends = env:esp32s2_saola' >>platformio.ini
        echo 'build_flags = ${env:esp32s2_saola.build_flags} ${common.debug_flags}' >>platformio.ini
        
        pio run -e custom_build || pio run -e custom_build || true
    - name: Copy Files esp32s2_saola
      run: |
        cd $GITHUB_WORKSPACE
        WLEDFOLDERNAME=`cat /tmp/wledfoldername`
        cp ${WLEDFOLDERNAME}/build_output/firmware/custom_build.bin $GITHUB_WORKSPACE/custom_build_esp32s2_saola.bin || true
        date -I | tr -d '\n' >$GITHUB_WORKSPACE/last_update_date.txt
    - name: Commit Files esp32s2_saola
      run: |
        cd $GITHUB_WORKSPACE
        git config user.name "GitHub Actions Bot"
        git config user.email "<>"
        git add $GITHUB_WORKSPACE/custom_build_esp32s2_saola.bin || true
        git add $GITHUB_WORKSPACE/last_update_date.txt || true
        git commit -m "Commit Custom Build" || true
        git push origin main || true
